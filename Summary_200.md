# STM32F407 LED 控制实验 · 200字总结

## 实验内容
基于 HAL 库实现 STM32F407 三个 LED 的控制：开发板两个 LED 交替流水灯效果（300ms 间隔），扩展板一个 LED 闪烁（500ms 周期）。

## 核心实现
1. **LED_Init()** — 开启 GPIO 时钟，配置三路推挽输出，初始化为熄灭
2. **LED_On/Off/Toggle()** — 因硬件为低电平点亮，故在操作函数内反向处理
3. **主循环定时** — 采用软件定时器模式，基于 HAL_GetTick() 差值法实现两个独立计时器

## 调试关键点
- **GPIO 时钟必须开启**，否则寄存器无响应
- **理解硬件设计**，区分低/高电平点亮逻辑
- **模块分离**，避免 LED 操作与编码器 ISR 冲突
- **清理构建缓存**（rm build/Debug），确保最新代码上板

## 收获
熟练掌握 HAL 库 API、GPIO 初始化流程、软件定时设计。编译通过（6.5KB），三项功能验证正确。

**编译状态**：✅ 成功 | **测试结果**：✅ 通过 | **代码质量**：无警告无错误
