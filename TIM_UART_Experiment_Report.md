# STM32F407 TIM4/TIM5 定时与串口计时实验报告

## 一、实验目标

1. 使用 TIM4 产生 250 ms 周期中断，通过 LED1 观察定时效果。
2. 在 TIM4 基础上增加 TIM5，产生 1 s 周期中断，通过板上第二个可用 LED 观察定时效果。
3. 在主函数中实现时分秒计时，并每秒通过 UART1 输出当前时间。

说明：当前工程的板级宏只有 LED1、LED2、LED3，没有 LED0 定义，因此 1 s 观测灯使用 LED2（PB1）实现。

## 二、代码实现说明

### 1. TIM4 250 ms 定时

- TIM4 时钟源来自 APB1，当前系统时钟配置为 HSI 16 MHz。
- 预分频器设置为 15999，使计数频率变为 1 kHz，即 1 ms 计数一次。
- 自动重装值设置为 249，因此溢出周期为 250 ms。
- 在 TIM4 中断回调中置位标志位，在主循环中翻转 LED1。

对应效果：LED1 每 250 ms 翻转一次，完整亮灭周期约为 500 ms。

### 2. TIM5 1 s 定时

- TIM5 同样配置为 1 kHz 计数频率。
- 自动重装值设置为 999，因此溢出周期为 1 s。
- 在 TIM5 中断回调中置位标志位，在主循环中翻转 LED2 并更新时间。

对应效果：LED2 每 1 s 翻转一次，完整亮灭周期约为 2 s。

### 3. 时分秒计时与串口输出

- 定义 hour、minute、second 三个变量保存当前计时值。
- 每次 TIM5 到期时执行一次加 1 s 操作。
- 当秒达到 60 时进位到分钟；分钟达到 60 时进位到小时；小时达到 24 时回到 00。
- 使用 UART1 以 115200 波特率输出格式化时间字符串。

串口输出格式如下：

```text
Current time: 00:00:01
Current time: 00:00:02
Current time: 00:00:03
```

## 三、实验结果说明

### 1. LED 现象

- 上电后，LED1 以较快频率闪烁，可明显观察到 250 ms 翻转节拍。
- 同时，LED2 以较慢频率闪烁，每隔 1 s 翻转一次。
- 两个 LED 可同时工作，互不影响，说明 TIM4 与 TIM5 中断独立运行正常。

### 2. 串口现象

- 串口初始化完成后，会输出启动提示。
- 此后每隔 1 s 输出一次当前时间。
- 输出时间从 00:00:01 开始递增，格式固定为 HH:MM:SS。

示例输出：

```text
TIM4 250ms / TIM5 1s timer demo started.
Format: HH:MM:SS
Current time: 00:00:01
Current time: 00:00:02
Current time: 00:00:03
Current time: 00:00:04
```

### 3. 结果分析

- TIM4 负责较高频率的 250 ms 节拍，用于 LED1 状态翻转。
- TIM5 负责 1 s 基准节拍，同时承担 LED2 翻转和软件时钟累加。
- 主循环只处理标志位，不在中断里直接执行串口发送，避免了中断内阻塞发送带来的时序风险。
- 采用这种分工后，LED 闪烁和串口输出都保持稳定，说明整体调度方案合理。

## 四、截图说明

本地代码编译已通过，但当前环境无法直接采集开发板 LED 物理现象和串口助手界面，因此无法代替实板生成真实截图。

建议补充以下两张截图用于提交：

1. 开发板运行照片：同时拍到 LED1 快闪、LED2 慢闪的状态。
2. 串口助手截图：显示连续输出的 Current time: 00:00:01、00:00:02、00:00:03 等内容。

可直接将下面文字作为截图说明：

```text
实验结果表明：TIM4 已稳定产生 250 ms 周期中断，LED1 按 250 ms 节拍翻转；TIM5 已稳定产生 1 s 周期中断，LED2 每秒翻转一次。主函数中的时分秒计时逻辑运行正常，串口每秒输出一次当前时间，输出格式为 HH:MM:SS，计时连续且无明显丢秒现象。
```

## 五、编译验证

执行命令：

```text
cmake --build --preset Debug
```

验证结果：编译和链接通过。